[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "sentineltrace-mcp-server"
version = "0.1.0"
description = "SentinelTrace MCP Server — Security Tool Bridge for AI Agents"
requires-python = ">=3.12"

dependencies = [
    "mcp>=1.0.0",
    "httpx>=0.27.0",
    "python-dotenv>=1.0.0",
]
